[package]
name = "sha256-provider"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
sha2 = { version = "0.10", default-features = false }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
