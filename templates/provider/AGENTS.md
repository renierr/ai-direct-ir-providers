# Provider Rules

- Never install or change tooling without explicit user consent.
- Preserve upstream provenance, source hashes, licenses, and patch records.
- Keep the WIT contract small, typed, versioned, and application-facing.
- Do not publish generated artifacts before they are reproducible and hashed.
- Run the package validation and consumer conformance test before release.
